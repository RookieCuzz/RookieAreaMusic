# 更新日志

## 1.2.0 - 2026-08-12

### 新功能

- 新增 `/am area show <世界> <区域ID>`，可用仅执行玩家可见的粒子显示已保存区域的完整轮廓，再次执行可关闭预览。
- 游戏内 ROI 编辑器的新切片改为由首次右键方块的 `blockY` 决定 Y，不再依赖执行创建命令或使用“下一切片”时玩家的飞行高度；已有切片保留原 Y。

## 1.1.0 - 2026-08-10

### 新功能

- `enter_once` 区域新增 `enterCommands`，可在实际入选频道层位时以控制台身份按顺序调用原版或其他插件命令，也支持无音乐的 command-only 区域。
- 新增 activation token 配对的 `exitCommands`：只有真正入选且至少一条入场命令成功派发，才会在玩家走出、传送离开、切换世界或重载后区域不再命中时执行一次冻结离场动作；掉线与停服默认不执行。
- 新增玩家、世界、区域和坐标内置占位符，并可选对接 PlaceholderAPI 展开其他插件提供的 `%...%` 占位符。

### 安全与可靠性

- `enterCommands`、`exitCommands` 分别限制为每区最多 16 条、单条最多 1024 个字符、每个数组合计最多 8192 个字符；拒绝换行、NUL、超长内容和多余前导斜杠。
- 每个配置字符串只派发一条 Bukkit 命令，不按分号、`&&` 或管道拆分，也不调用系统 Shell；单条失败与未解析占位符会被隔离且不会重试。
- 命令只在实际进入且入选 `maxLayers` 层位时执行；曲目循环、玩家保持在同一区域时的配置重载和 CraftEngine 资源包声音补播不会重复触发副作用。

### 工程

- Java 包与 Maven 坐标统一迁移到 `io.github.rookiecuzz` 命名空间，插件运行时作者信息只保留当前维护者。
- 移除已废弃的旧数据目录自动迁移逻辑；当前数据目录保持为 `plugins/RookieAreaMusic/`。
- 管理权限节点统一为 `rookieareamusic.admin`；使用权限插件的服务器需要同步更新授权规则。

## 1.0.0 - 2026-08-09

### 新功能

- 提供 CT ROI 式逐层多边形区域编辑器，支持私有粒子预览、逐层复制、撤销、完成与取消回滚。
- 提供 BGM 独占、Ambience 多层叠加、Stinger 入场触发以及固定坐标音源。
- 支持 CraftEngine Sound Event 与资源包工作流，并附带多声道声音模板和完整配置指南。

### 修复

- 区域曲目增删改为单区域原子写入，写盘失败时不会污染内存或其他区域文件。
- 修复异步位置任务、玩家退出和声音输出之间的状态竞争，确保逻辑会话与客户端声音按玩家 FIFO 保持一致。
- 区域音乐改用跟随玩家的 `MUSIC` 类别，固定音源使用 `AMBIENT` 类别，避免同键声音互相误停。
- 修复固定音源删除、禁用、重载、播放失败重试及同键音源补播的生命周期问题。
- 为切片数、单层顶点数和总顶点数增加安全上限，并拒绝小数 Y、自相交与零面积轮廓。
- 修复编辑工具在满快捷栏、创造模式、Bundle、掉落、死亡和容器交互中的可用性与残留问题。

### 文档与工程

- 增加 GitHub Actions 构建、79 项自动化测试、Java 8 字节码校验和 MIT 许可证打包校验。
- 补充安装说明、区域与音乐配置参考、CraftEngine 声音扩展指南及原项目版权归属。
