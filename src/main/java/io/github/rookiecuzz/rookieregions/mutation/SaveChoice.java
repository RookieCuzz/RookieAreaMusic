package io.github.rookiecuzz.rookieregions.mutation;

/** The exact server-authorized action taken after placement evaluation. */
public enum SaveChoice {
    DIRECT,
    SET_PARENT,
    KEEP_OVERLAP
}
